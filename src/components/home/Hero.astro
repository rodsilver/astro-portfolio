---
import { IHeroProps } from '@types';

const { name, about } = Astro.props as IHeroProps;
---

<section>
  <p class='dark:text-light mt-[117px]'>Hola, I’m</p>
  <h1 class='leading-normal my-0'>{name}</h1>
  <p class='dark:text-light text-lg mb-16 text-justify'>
    {about}
  </p>
</section>
